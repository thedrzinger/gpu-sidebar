# Node
node_modules/
*.tgz

# Build output — the bundled TUI entry (regenerated by `npm run build`;
# the npm tarball gets a fresh one via prepublishOnly)
dist/

# Third-party reference material (docs snapshot + example plugin source).
# Regenerate with: bash scripts/fetch-reference.sh
/reference/

# Local design reference (user-provided screenshot), not part of the project
bargraphimage.png

# OS / editor noise
.DS_Store

# Local config + backup files
tui.json
*.bak

# Personal working list, not part of the project
todo.md

# Local agent instructions (kept out of the repo)
AGENTS.md

# Project progress log (local notes, kept out of the repo)
gpu-sidebar-progress.md

# Python
__pycache__/
*.pyc
